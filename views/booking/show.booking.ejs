<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Booking Details</title>
    </head>
    <body>
        <h1>Booking Details</h1>
        <p><strong>Renter:</strong> <%= booking.renter.name %></p>
        <p><strong>Property:</strong> <%= booking.property.title %></p>
        <p><strong>Start Date:</strong> <%= booking.startDate.toISOString().substr(0, 10) %></p>
        <p><strong>End Date:</strong> <%= booking.endDate.toISOString().substr(0, 10) %></p>
        <p><strong>Total Price:</strong> <%= booking.totalPrice %></p>
        <a href="/booking/<%= booking._id %>/edit">Edit Booking</a>
    </body>
</html>