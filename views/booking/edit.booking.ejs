<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Booking</title>
  </head>
  <body>
    <h1>Edit Booking</h1>
    <form action="/booking/<%= booking._id %>?_method=PATCH" method="POST">
      <label for="renter">Renter:</label>
      <input type="text" id="renter" name="renter" value="<%= booking.renter %>" required><br>

      <label for="property">Property:</label>
      <input type="text" id="property" name="property" value="<%= booking.property %>" required><br>

      <label for="startDate">Start Date:</label>
      <input type="date" id="startDate" name="startDate" value="<%= booking.startDate.toISOString().substr(0,10) %>" required><br>

      <label for="endDate">End Date:</label>
      <input type="date" id="endDate" name="endDate" value="<%= booking.endDate.toISOString().substr(0,10) %>" required><br>

      <label for="totalPrice">Total Price:</label>
      <input type="number" id="totalPrice" name="totalPrice" value="<%= booking.totalPrice %>" required><br>

      <button type="submit">Save</button>
    </form>
  </body>
</html>